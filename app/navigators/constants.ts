/**
 * Navigation-related constants
 */

/** Height of the bottom tab bar (excluding safe area insets) */
export const TAB_BAR_HEIGHT = 70

/** Height of the header (excluding safe area insets) */
export const HEADER_HEIGHT = 56
